import 'package:flutter/material.dart';

class ChangeAdress extends StatelessWidget {
  const ChangeAdress({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
